# Prontuário Luna Princess - Configurações de Ambiente
# Modo de desenvolvimento - usando SQLite como fallback

# === CONFIGURAÇÕES GERAIS ===
APP_ENV=development

# === BANCO DE DADOS ===
# PostgreSQL (se disponível)
PGHOST=localhost
PGDATABASE=prontuario_luna
PGUSER=postgres
PGPASSWORD=
PGPORT=5432

# URL do banco (será SQLite se PostgreSQL não estiver disponível)
DATABASE_URL=sqlite:///prontuario_luna.db

# === INTELIGÊNCIA ARTIFICIAL ===
# Substitua pela sua chave real da OpenAI
OPENAI_API_KEY=sk-your-openai-api-key-here

# === SEGURANÇA ===
# Chave de criptografia de 32 bytes (32 caracteres)
# IMPORTANTE: Gere uma chave única para produção!
ENCRYPTION_KEY=dev-key-32-chars-for-testing-only

# === CONFIGURAÇÕES OPCIONAIS ===
# SMTP (opcional)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_USE_TLS=true

# === NOTAS ===
# 1. Para produção, configure PostgreSQL adequadamente
# 2. Gere uma chave ENCRYPTION_KEY única e segura
# 3. Configure sua chave real da OpenAI
# 4. O sistema usará SQLite automaticamente se PostgreSQL não estiver disponível
